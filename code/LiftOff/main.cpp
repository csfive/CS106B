/*
 * File: main.cpp
 * --------------
 * Blank C++ project configured to use Stanford cslib and Qt
 */

#include "console.h"
#include "simpio.h"
using namespace std;


int main()
{
    int count = 10;
    while (count > 0) {
        cout << count << endl;
        count--;
    }
    cout << "Liftoff" << endl;
    return 0;
}
